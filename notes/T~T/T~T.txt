```lua
for i, v in pairs(Human:Nooby:Classmates) do
	v.kill()
end
```

// Lua is so weird.
// Why is everything onde-indexed?

just do:

```cpp
{
int i = 0;
for (auto& something : Something) {
	printf("This is index %d\n", i);
	++i;
}
}
```